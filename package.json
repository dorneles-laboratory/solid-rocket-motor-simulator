{
  "private": true,
  "scripts": {
    "ui:build": "cd ui && npm run build",
    "ui:lint": "cd ui && npm run lint",
    "ui:typecheck": "cd ui && tsc --noEmit",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.4"
  }
}
