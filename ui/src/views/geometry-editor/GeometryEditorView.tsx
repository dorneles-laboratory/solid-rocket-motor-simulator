import ComingSoon from '../../components/cooming-soon/coming-soon';
import styles from './GeometryEditorView.module.css';

export default function GeometryEditorView() {
  return (
    <main className={styles.geometry_editor_view}>
      <ComingSoon />
    </main>
  )
}